{
  "person": false,
  "place": false,
  "expression": "contact.type === 'person'  && !summary.deceased && (!contact.sex || contact.sex === 'female') && (!contact.date_of_birth || (ageInYears(contact) >= 10 && ageInYears(contact) < 50))"
}
