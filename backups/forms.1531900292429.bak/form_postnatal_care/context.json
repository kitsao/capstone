{
  "person": true,
  "place": false,
  "expression": "user.parent.type !== 'district_hospital' && (!contact || (contact.type === 'person'  && !summary.deceased && (!contact.sex || contact.sex === 'female') && (!contact.date_of_birth || (ageInYears(contact) >= 10 && ageInYears(contact) < 50))))"
}
